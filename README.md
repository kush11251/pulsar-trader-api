# Pulsar Trader API
A fast and reliable trading platform API built using Go.

## Getting Started
To get started, run the following command to initialize the Go module and download dependencies:
```bash
go mod init pulsar-trader-api
```
Then, run the following command to start the server:
```bash
go run src/main.go
```
